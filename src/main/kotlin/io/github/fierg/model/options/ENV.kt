package io.github.fierg.model.options

enum class ENV {
    STATE,
    CLEAN,
    COROUTINES,
    MODE,
    DELTA_WINDOW_PREPROCESSING,
    DELTA_WINDOW_ALGO,
    SKIP_SINGLE_STEP_EDGES,
    DEBUG,
    QUIET,
}