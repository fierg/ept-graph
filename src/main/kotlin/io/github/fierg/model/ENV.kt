package io.github.fierg.model

enum class ENV {
    STATE,
    CLEAN,
    COROUTINES,
    MODE,
    DELTA_WINDOW_PREPROCESSING,
    DELTA_WINDOW_ALGO,
    SKIP_SINGLE_STEP_EDGES,
    DEBUG,
    QUIET,
}